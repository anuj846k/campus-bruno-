info:
  name: Swagger JSON
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/swagger.json"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
