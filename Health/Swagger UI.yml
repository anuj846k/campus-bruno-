info:
  name: Swagger UI
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api-docs"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
