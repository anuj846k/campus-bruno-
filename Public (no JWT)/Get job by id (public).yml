info:
  name: Get job by id (public)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/jobs/listById-public/{{jobId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: Includes description. 404 when the job is not status = active.
