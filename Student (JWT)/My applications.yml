info:
  name: My applications
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/jobs/my-applications?limit=50&offset=0"
  params:
    - name: limit
      value: "50"
      type: query
      description: Default 50, capped at 100.
    - name: offset
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
