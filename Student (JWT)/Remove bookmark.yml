info:
  name: Remove bookmark
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/jobs/bookmark/{{jobId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
